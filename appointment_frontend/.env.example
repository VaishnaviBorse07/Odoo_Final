# API (FastAPI)
VITE_API_URL=http://localhost:8000/api

# Razorpay Key ID only (safe in browser). Must match test/live key pair on server.
# Leave unset to use one-click mock pay (PUT /bookings/:id/pay) for local demos.
VITE_RAZORPAY_KEY_ID=rzp_test_xxxxxxxx
